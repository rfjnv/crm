-- Параллельное кол-во в рулонах для движений склада (товары с двойным учётом кг+рулоны,
-- напр. ламинационная плёнка). Раньше рулоны при приходе попадали только в текст note,
-- а при списании по сделке не сохранялись вовсе — в истории движений их не было видно.
-- NULL — движение по товару без рулонного учёта.
ALTER TABLE "inventory_movements" ADD COLUMN IF NOT EXISTS "roll_quantity" DECIMAL(12,3);
