[build]
command = "cd frontend && npm ci && npm run build && cd .. && node scripts/copy-dist.js"
publish = "public"

[[redirects]]
from = "/*"
to = "/index.html"
status = 200

[context.production]
environment = { NODE_ENV = "production" }

[context.deploy-preview]
environment = { NODE_ENV = "production" }
