{
  "name": "crm-root",
  "private": true,
  "engines": {
    "node": "18.x"
  },
  "scripts": {
    "heroku-postbuild": "cd backend && npm install --include=dev && npm run build",
    "build": "cd backend && npm install --include=dev && npx prisma generate && npm run build",
    "postinstall": "cd backend && npm install --include=dev || true",
    "start": "cd backend && npm run start",
    "copy-dist": "rimraf public && mkdir -p public && cp -r frontend/dist/* public/"
  },
  "devDependencies": {
    "rimraf": "^6.1.3"
  }
}
