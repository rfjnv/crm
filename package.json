{
  "name": "crm-root",
  "private": true,
  "engines": {
    "node": ">=18.x"
  },
  "scripts": {
    "build": "cd backend && npm install --include=dev && npx prisma generate && npx prisma db push --skip-generate && npm run build",
    "postinstall": "cd backend && npm install --include=dev",
    "start": "cd backend && npm run start"
  }
}
